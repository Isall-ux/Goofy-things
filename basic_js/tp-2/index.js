const nilai = 50;
const name = 'mas eko';

function lulus(){
    if(nilai > 75){
        console.log(`selamat ${name} maneh lulus euy, mahal mahal, nilai mane ${nilai}`)
    }else{
        console.log(`eleuh sia ${name} meni bodo bodo teing, naha bisa meunang nilai ${nilai} ai mane`)
    }
}

lulus()